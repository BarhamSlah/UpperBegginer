public → Classes and methods that should be accessible from outside.
private → Fields/instance variables for encapsulation (and private methods when only the class needs them).
protected → Use when subclasses need access to something from the superclass.
default (no keyword) → Use when something only needs to be accessible within the same package.

public method  → Other classes need to use it.
private method → Only this class needs to use it.  
